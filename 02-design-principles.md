# Part 2: 設計指針

> **このPartでは**: プロジェクト開始時に決めるべきドキュメント構成とアーキテクチャについて解説します。
> 
> **読むタイミング**: プロジェクト開始時、設計レビュー時

### レベル別ガイド

| レベル | このPartで読むべきセクション |
|-------|---------------------------|
| **L1(基礎)** | 7. ドキュメントサイズ、11. コロケーション、まとめ |
| **L2(実践)** | L1 + 8. モジュール分割、9. アーキテクチャ、10. 型設計、12. 併用 |
| **L3(最適化)** | 全体(10. 型設計の高度な部分:Branded Types、zod SSoT含む) |

### このPartと哲学の階層構造

| Level | 哲学 | このPartでの実践 |
|-------|------|-----------------|
| L0 目的 | **持続可能性** | 知識をドキュメント・型・構造に蓄積し、人に依存しない |
| L1 環境 | **心理的安全性** | 明確な構造により「どこを見ればいいか」がわかる安心感 |
| L2 態度 | **急がば回れ** | 設計段階で型を定義、コロケーションで迷わない構造を作る |
| L3 手段 | **シンプルさ** | ドキュメント・ファイルを小さく分割、型で曖昧さを排除 |

### 3行で言うと

1. **ドキュメントは小さく、関連するものは近くに**
2. **1ファイル200-300行目安、モジュール単位で分割**
3. **型はClaudeへの仕様書 — Branded Types、Result型、zodで安全に**

> 💡 以下の詳細は読み飛ばしてもOK。迷ったときに戻ってきてください。

---

```
Part 2 の流れ:
  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
  │ 7. ドキュ   │ ──→ │ 8. モジュール│ ──→ │ 9. アーキ   │
  │    メント   │     │    単位分割  │     │    テクチャ │
  └─────────────┘     └─────────────┘     └─────────────┘
        │                                       │
        └───────────────────┬───────────────────┘
                            ↓
                  ┌─────────────────┐
                  │ 10. 型設計指針  │ ← NEW
                  └─────────────────┘
                            ↓
                  ┌─────────────────┐
                  │ 11. コロケーション│
                  └─────────────────┘
                            ↓
                  ┌─────────────────┐
                  │ 12. アーキテクチャ│
                  │     との併用    │
                  └─────────────────┘
```

---

## 7. ドキュメントの適切なサイズとコンテキスト管理 [L1]

### Claudeのコンテキスト特性

> 💡 以下の数値は**実践的な目安**です。Anthropic公式の推奨値ではなく、
> コミュニティの経験則に基づいています。プロジェクトの特性に応じて調整してください。

```
┌─────────────────────────────────────────────────────────────────┐
│                   コンテキストウィンドウ                         │
│                                                                 │
│  技術的限界     ████████████████████████████  〜200kトークン    │
│                 (Anthropic公式:標準プラン)                    │
│                                                                 │
│  実用的限界     ████████████████              〜5万文字         │
│                 (これ以上はパフォーマンス低下の傾向)           │
│                                                                 │
│  推奨範囲       ████████                      〜3万文字         │
│                 (1回の会話での合計・目安)                      │
│                                                                 │
│  1ドキュメント  █████                         〜4,000文字       │
│                 (GitHub Copilot推奨に準拠)                     │
└─────────────────────────────────────────────────────────────────┘
```

### ドキュメント種別ごとの推奨サイズ

| ドキュメント種別 | 推奨文字数 | ポイント |
|-----------------|-----------|----------|
| CLAUDE.md | 1,000〜3,000 | 毎回読まれる、軽量に(Boris Cherny氏:約2.5kトークン推奨) |
| 参照ドキュメント(1ファイル) | 〜4,000 | GitHub Copilot公式推奨に準拠 |
| 1回の会話での読み込み合計 | 〜30,000 | 経験則(パフォーマンス低下を避ける目安) |

> ※「1回の会話での読み込み合計」= CLAUDE.md + 参照指示したドキュメントの合計
>
> 📚 参考: [GitHub Blog - Copilot Instructions Files](https://github.blog/ai-and-ml/unlocking-the-full-power-of-copilot-code-review-master-your-instructions-files/)
> "If the file is over 4000 characters long, prioritize shortening the file"

### Lost in the Middle 問題

LLMは長いコンテキストの「中間部分」の情報を見落としやすいという研究結果があります。

```
┌─────────────────────────────────────────────────────────────────┐
│                    記憶の保持度                                  │
│                                                                 │
│  保持度                                                         │
│    ↑                                                            │
│  高 │ ◉                                           ○             │
│    │   ◉                                       ○                │
│    │     ◉                                   ○                  │
│    │       ◉                               ○                    │
│    │         △   △   △   △   △   △   △                        │
│  低 │           △       △       △                               │
│    └────────────────────────────────────────────→ 位置          │
│        先頭      中間部分           末尾                         │
│        ◎        △                ○                             │
│      よく覚える  忘れやすい      まあ覚える                      │
│                                                                 │
│  対策: 重要な情報は先頭か末尾に配置                              │
└─────────────────────────────────────────────────────────────────┘
```

> 📚 参考文献: Liu et al. (2023) "Lost in the Middle: How Language Models Use Long Contexts"
> https://arxiv.org/abs/2307.03172

### コンテキストウィンドウ管理戦略

#### いつ新しい会話を始めるか

```
┌─────────────────────────────────────────────────────────────────┐
│              新しい会話を始めるべきタイミング                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ✅ 新しい会話を始める                                           │
│  ─────────────────────                                          │
│  ・別の機能/モジュールの作業に移る時                             │
│  ・会話が長く続いた時(パフォーマンス低下を感じたら)            │
│  ・Claudeが以前の指示と矛盾する回答をし始めた時                  │
│  ・大きなリファクタリングを始める時                              │
│  ・日をまたいで作業を再開する時                                  │
│                                                                 │
│  ❌ 同じ会話を継続する                                           │
│  ─────────────────────                                          │
│  ・同じ機能の実装を継続中                                        │
│  ・エラー修正のイテレーション中                                  │
│  ・直前の実装に対するテスト作成                                  │
│  ・関連する小さな修正を連続で行う時                              │
│                                                                 │
│  💡 Claude Code公式: "/clear: Reset context between unrelated   │
│     tasks. Long sessions with irrelevant context can reduce     │
│     performance."                                               │
└─────────────────────────────────────────────────────────────────┘
```

#### 会話を跨いで引き継ぐ情報

```
┌─────────────────────────────────────────────────────────────────┐
│                  情報の引き継ぎ方法                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  引き継ぐ情報            方法                                   │
│  ────────────            ────                                   │
│  プロジェクト設定        → CLAUDE.md に記載(常に参照される)   │
│  決定事項・方針          → CLAUDE.md または docs/ に記録        │
│  禁止事項・学び          → CLAUDE.md の禁止事項セクション       │
│  現在のタスク状況        → 新会話の最初に明示的に伝える         │
│  直前の実装内容          → 該当ファイルを読ませる               │
│                                                                 │
│  新会話開始時のテンプレート:                                    │
│  ─────────────────────────                                      │
│  「○○機能の実装を続けます。                                     │
│                                                                 │
│   前提:                                                        │
│   ・CLAUDE.md を参照してください                                 │
│   ・前回、src/modules/auth/login.ts を実装済み                  │
│   ・今回:ログアウト機能を追加します                             │
│                                                                 │
│   まず login.ts を読んで、実装方針を確認してください」           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 長い会話での対策

```
┌─────────────────────────────────────────────────────────────────┐
│                長い会話でのコンテキスト維持                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  会話が長くなった時の症状:                                       │
│  ・以前決めた命名規則を忘れる                                    │
│  ・同じエラーを繰り返す                                          │
│  ・矛盾した提案をする                                            │
│                                                                 │
│  対策:                                                          │
│                                                                 │
│  1. 定期的なリマインド(10-15ターンごと)                        │
│     「念のため確認:CLAUDE.md の命名規則に従っていますか?」     │
│                                                                 │
│  2. 重要な決定の即時記録                                         │
│     決定 → すぐにCLAUDE.mdに追記 → 次の指示                      │
│                                                                 │
│  3. サマリーの挿入                                               │
│     「ここまでの実装状況:                                       │
│      ✅ User エンティティ                                        │
│      ✅ ログインAPI                                              │
│      🔄 ログアウトAPI(今ここ)                                  │
│      ⬜ トークンリフレッシュ」                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 効果的なアプローチ

```
❌ やりがちな失敗:
  50ページの要件定義書をすべてコンテキストに入れる
  → トークン消費大、重要な部分が埋もれる

✅ 効果的なアプローチ:
  CLAUDE.md(1-2ページ): 常に参照する核心情報
  /docs/requirements.md: 必要時に「これを読んで」と指示
```

### コンテキストのコスト最適化

API利用時はトークン数に応じた課金が発生するため、コスト効率も意識する必要があります。

```
┌─────────────────────────────────────────────────────────────────┐
│                  コスト最適化の3原則                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 不要なファイルを除外する                                    │
│     → .claudeignore で依存関係やビルド成果物を除外              │
│                                                                 │
│  2. 必要な情報だけを読み込む                                    │
│     → モジュール単位で参照、全体を一度に読み込まない            │
│                                                                 │
│  3. CLAUDE.md を軽量に保つ                                      │
│     → 3,000文字以下、詳細は別ドキュメントに分離                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### .claudeignore の活用

プロジェクトルートに `.claudeignore` を配置し、不要なファイルを除外:

```
# 依存関係(大量のファイル)
node_modules/
vendor/
.venv/
__pycache__/

# ビルド成果物
dist/
build/
out/
*.min.js
*.min.css

# 大きなデータファイル
*.csv
*.xlsx
data/*.json

# ログファイル
*.log
logs/

# 機密情報(セキュリティ上も重要)
.env*
secrets/
*.pem
*.key
```

#### コンテキスト読み込みの優先順位

```
┌─────────────────────────────────────────────────────────────────┐
│              コンテキスト読み込みの優先順位                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  優先度    内容                      読み込みタイミング         │
│  ──────    ────                      ──────────────────         │
│                                                                 │
│  最高      CLAUDE.md                 常に(自動)               │
│                                                                 │
│  高        作業対象モジュール        タスク開始時               │
│            src/modules/auth/                                    │
│                                                                 │
│  中        関連モジュール            必要に応じて               │
│            src/modules/user/                                    │
│                                                                 │
│  低        共通コード                参照が必要な時のみ         │
│            src/shared/                                          │
│                                                                 │
│  除外      依存関係・ビルド成果物    読み込まない               │
│            node_modules/, dist/                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### コスト意識した指示の出し方

```
❌ コストが高い指示:
  「プロジェクト全体を読んで、問題がないか確認して」
  → 全ファイルを読み込み、大量のトークンを消費

✅ コストを抑えた指示:
  「src/modules/auth/ のログイン機能を確認して」
  → 必要なモジュールのみ読み込み
```

---

## 8. モジュール単位のドキュメント分割 [L2]

### なぜモジュール単位が効果的か

```
┌─────────────────────────────────────────────────────────────────┐
│                   作業単位とドキュメント単位の一致                │
│                                                                 │
│  Claudeへの指示              対応するドキュメント                │
│  ─────────────────           ─────────────────────              │
│  「認証機能を実装して」   →   docs/modules/auth/                 │
│  「研修進捗を作って」     →   docs/modules/training/             │
│  「レポート機能を追加」   →   docs/modules/report/               │
│                                                                 │
│  → すべて「モジュール単位」の作業                                │
│  → 必要な情報だけを正確に参照できる                              │
└─────────────────────────────────────────────────────────────────┘
```

### 推奨ディレクトリ構造

```
/docs
├── overview.md                  # プロジェクト全体像(〜3,000文字)
│
└── /modules                     # モジュール単位
    ├── /auth
    │   ├── requirements.md      # 要件(〜10,000文字)
    │   ├── api.md               # API仕様(〜5,000文字)
    │   └── database.md          # テーブル定義(〜3,000文字)
    │
    ├── /training
    │   ├── requirements.md
    │   ├── api.md
    │   └── database.md
    │
    └── /progress
        └── ...
```

### 分割の粒度判断

```
┌─────────────────────────────────────────────────────────────────┐
│                       分割粒度のスペクトラム                     │
│                                                                 │
│  粗すぎる              適切                細かすぎる           │
│     ↓                   ↓                    ↓                  │
│  ┌────────┐        ┌────────┐          ┌────────┐              │
│  │全機能  │        │1モジュール│        │1ボタン │              │
│  │20,000字│        │〜10,000字 │        │200字×50│              │
│  └────────┘        └────────┘          └────────┘              │
│     ↓                   ↓                    ↓                  │
│  結局全部          1機能=1モジュール     管理が煩雑             │
│  読む必要          =3〜5ファイル        全体像見えない          │
│                                                                 │
│                    ◎ ここを目指す                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 9. AI協働開発に適したアーキテクチャ [L2]

### Claudeの特性から逆算した条件

| 特徴 | 理由 |
|------|------|
| 明確な境界 | 「この範囲を実装して」が明確になる |
| 単方向の依存 | 影響範囲が予測しやすい |
| 小さな単位 | 1回の指示で完結できる |
| 型による契約 | インターフェースが明示的 |
| テスト容易性 | 検証フィードバックループが回せる |

### 推奨アーキテクチャ

```
┌─────────────────────────────────────────────────────────────────┐
│                  プロジェクト規模とアーキテクチャ                 │
│                                                                 │
│  規模        推奨アーキテクチャ                                  │
│  ────        ─────────────────                                  │
│                                                                 │
│  小規模      ┌─────────────────┐                                │
│  学習用      │  Vertical Slice │  機能単位で縦に分割            │
│              └─────────────────┘                                │
│                      ↓                                          │
│  中規模      ┌─────────────────┐                                │
│  チーム      │ Clean           │  層で分離、依存を制御          │
│              │ Architecture    │                                │
│              └─────────────────┘                                │
│                      ↓                                          │
│  大規模      ┌─────────────────┐                                │
│  複数        │ Modular Monolith│  モジュール境界 +              │
│  ドメイン    │ + Clean Arch    │  層の分離を併用                │
│              └─────────────────┘                                │
└─────────────────────────────────────────────────────────────────┘
```

### Claude特有の最適化ポイント

| 観点 | 一般的最適化 | Claude最適化 |
|------|-------------|-------------|
| ファイルサイズ | 論理的にまとめる | 物理的に分割(300行以下) |
| 型定義 | 集約してもOK | 使用箇所の近くに配置 |
| ドキュメント | 共有フォルダに集約 | 機能の近くに配置 |
| 設計意図 | 口頭伝達OK | コメントやADRで明示的に記録 |

---

## 9.5 設計原則(SOLID等)のAI協働適用 [L2]

従来のソフトウェア設計原則は、AI協働開発でも有効です。
ただし、AI協働の文脈で**再解釈**することで、より実践的に適用できます。

### 疎結合と高凝集 — モジュール設計の基本

#### 定義

> **疎結合(Loose Coupling)**: モジュール間の依存を最小化する
> **高凝集(High Cohesion)**: モジュール内の関連性を最大化する

```
┌─────────────────────────────────────────────────────────────────┐
│                疎結合と高凝集の関係                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  理想的なモジュール設計:                                        │
│                                                                 │
│  ┌─────────┐    疎結合    ┌─────────┐                          │
│  │ Module A │◄──────────►│ Module B │                          │
│  │ ┌─────┐ │  (依存少)   │ ┌─────┐ │                          │
│  │ │func1│ │             │ │func3│ │                          │
│  │ │func2│ │             │ │func4│ │                          │
│  │ └─────┘ │             │ └─────┘ │                          │
│  │ 高凝集  │             │ 高凝集  │                          │
│  │(関連強) │             │(関連強) │                          │
│  └─────────┘             └─────────┘                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### AI協働での意義

| 原則 | AI協働での効果 |
|------|---------------|
| **疎結合** | タスク分割が容易、独立して検証可能、並列作業が可能 |
| **高凝集** | 1タスク=1モジュール、コンテキストが明確、AIの理解が容易 |

#### 実践チェックリスト

- [ ] このモジュールは他のモジュールなしでテストできるか?(疎結合)
- [ ] このモジュール内の関数は同じ目的を持つか?(高凝集)
- [ ] モジュール間の依存はインターフェースを通しているか?(疎結合)
- [ ] 変更時に影響範囲が限定されるか?(疎結合)

### 関心の分離(Separation of Concerns)

#### 定義

> **定義**: プログラムを異なる関心(責務)ごとに分離し、
> 各部分が1つの関心のみを扱うようにする設計原則

```
┌─────────────────────────────────────────────────────────────────┐
│                  関心の分離の例                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ❌ 関心が混在(悪い例)                                        │
│  ─────────────────────                                          │
│  function processOrder(order) {                                 │
│    // 検証、計算、DB保存、メール送信が混在                      │
│  }                                                              │
│                                                                 │
│  ✅ 関心を分離(良い例)                                        │
│  ─────────────────────                                          │
│  validateOrder(order)     // 検証の関心                         │
│  calculateTotal(order)    // 計算の関心                         │
│  saveOrder(order)         // 永続化の関心                       │
│  notifyCustomer(order)    // 通知の関心                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### AI協働での適用

| 場面 | 関心の分離の実践 |
|------|-----------------|
| **タスク分解** | 1タスク = 1関心(検証、実装、テストを分離) |
| **プロンプト設計** | 1プロンプト = 1目的(複数の目的を混ぜない) |
| **ファイル構成** | 1ファイル = 1責務(S2原則と同義) |

> **S2「1ファイル=1責務」との関係**: 関心の分離は、S2原則の理論的基盤です。

### 依存性逆転原則(DIP: Dependency Inversion Principle)

#### 定義

> **定義**: 上位モジュールは下位モジュールに依存すべきでない。
> 両者は抽象(インターフェース)に依存すべき。

```
┌─────────────────────────────────────────────────────────────────┐
│                  依存性逆転の図解                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ❌ 従来(上位が下位に依存)                                    │
│  ─────────────────────────                                      │
│  OrderService ──────────► MySQLRepository                       │
│  (具体的な実装に依存)                                         │
│                                                                 │
│  ✅ DIP適用後(両者が抽象に依存)                               │
│  ─────────────────────────────────                              │
│  OrderService ───► IRepository ◄─── MySQLRepository             │
│  (インターフェースに依存)                                     │
│                                                                 │
│  効果: 実装を差し替え可能(テスト時にモック、本番でMySQL)      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### AI協働での意義

| 観点 | DIPの効果 |
|------|----------|
| **テスト容易性** | モックに差し替えて単体テスト可能 |
| **タスク分離** | インターフェース定義と実装を別タスクに |
| **並列作業** | インターフェースが決まれば実装は並列可能 |

#### TypeScriptでの実践

```typescript
// インターフェース定義(抽象)
interface UserRepository {
  findById(id: UserId): Promise<User | null>;
  save(user: User): Promise<void>;
}

// 本番実装
class PostgresUserRepository implements UserRepository {
  async findById(id: UserId): Promise<User | null> { /* ... */ }
  async save(user: User): Promise<void> { /* ... */ }
}

// テスト用モック
class InMemoryUserRepository implements UserRepository {
  private users: Map<string, User> = new Map();
  async findById(id: UserId): Promise<User | null> {
    return this.users.get(id) ?? null;
  }
  async save(user: User): Promise<void> {
    this.users.set(user.id, user);
  }
}
```

### インターフェース分離原則(ISP: Interface Segregation Principle)

#### 定義

> **定義**: クライアントは、使用しないメソッドへの依存を強制されるべきでない。
> 大きなインターフェースより、小さく特化したインターフェースを優先する。

```
┌─────────────────────────────────────────────────────────────────┐
│                  インターフェース分離の例                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ❌ 肥大化したインターフェース                                  │
│  ─────────────────────────────                                  │
│  interface Worker {                                             │
│    work(): void;                                                │
│    eat(): void;                                                 │
│    sleep(): void;                                               │
│  }                                                              │
│  // ロボットは eat() と sleep() を実装できない                  │
│                                                                 │
│  ✅ 分離されたインターフェース                                  │
│  ─────────────────────────────                                  │
│  interface Workable { work(): void; }                           │
│  interface Eatable { eat(): void; }                             │
│  interface Sleepable { sleep(): void; }                         │
│  // 必要なインターフェースのみ実装                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### AI協働での適用

- **型設計**: 大きな型より、合成可能な小さな型
- **API設計**: 汎用APIより、目的特化API
- **タスク分解**: 大きなタスクより、単一目的のタスク

### 開放閉鎖原則(OCP: Open/Closed Principle)

#### 定義

> **定義**: ソフトウェアは拡張に対して開かれ、修正に対して閉じているべき。
> 既存コードを変更せずに、新機能を追加できる設計。

```
┌─────────────────────────────────────────────────────────────────┐
│                  開放閉鎖原則の適用                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ❌ OCP違反(新しい型を追加するたびに修正が必要)               │
│  ─────────────────────────────────────────                      │
│  function calculateArea(shape: Shape) {                         │
│    if (shape.type === 'circle') return ...;                     │
│    if (shape.type === 'square') return ...;                     │
│    // 新しい図形を追加するたびにこの関数を修正                  │
│  }                                                              │
│                                                                 │
│  ✅ OCP準拠(拡張で対応)                                       │
│  ─────────────────────────                                      │
│  interface Shape {                                              │
│    calculateArea(): number;                                     │
│  }                                                              │
│  class Circle implements Shape { calculateArea() { ... } }      │
│  class Square implements Shape { calculateArea() { ... } }      │
│  // 新しい図形はクラスを追加するだけ、既存コードは変更不要      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### AI協働での意義

| 観点 | OCPの効果 |
|------|----------|
| **変更の局所化** | 新機能追加時に既存コードを触らない |
| **テストの安定** | 既存テストが壊れにくい |
| **並列作業** | 既存コードと新機能を独立して開発可能 |

### SOLID原則とフレームワーク原則の対応

| SOLID原則 | フレームワーク原則 | 関係 |
|-----------|-------------------|------|
| SRP(単一責任) | S2「1ファイル=1責務」 | 同義 |
| OCP(開放閉鎖) | R2「プロセスの可逆性」 | 変更しやすい設計 |
| LSP(リスコフ置換) | A4「規約の厳守」 | インターフェース遵守 |
| ISP(インターフェース分離) | S1「1文1検証」 | 小さく分割 |
| DIP(依存性逆転) | 疎結合設計 | テスト容易性 |

---

## 10. 型設計指針:型で設計を表現する [L2]

### 核心:型は「仕様書」であり「設計図」

型は単なるエラー検出ツールではない。
**型はClaudeに対する明確な仕様書**であり、曖昧な自然言語より正確に意図を伝える。
さらに進んで、**型で設計そのものを表現する**ことで、コンパイラが設計違反を検出できるようになる。

```
┌─────────────────────────────────────────────────────────────────┐
│                    型の4つの役割                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. Claudeへの仕様伝達                                          │
│     → 曖昧な自然言語より、型定義で意図を正確に伝える             │
│                                                                 │
│  2. 幻覚の検出                                                  │
│     → 存在しないAPIを生成したら型エラーで即座に検出             │
│                                                                 │
│  3. 設計の表現                                                  │
│     → 状態遷移、不変性、境界を型で定義し、違反をコンパイル時検出 │
│                                                                 │
│  4. 実行前の検証                                                │
│     → コンパイル時にバグを発見、実行時エラーを防ぐ              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 型設計の原則:「型で表現できることは型で表現する」

```
┌─────────────────────────────────────────────────────────────────┐
│              型設計のレベル                                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Level 1: エラー検出                                            │
│  ─────────────────                                              │
│  「型エラーでバグを見つける」                                    │
│  → 基本的な型付け、any禁止                                      │
│                                                                 │
│  Level 2: 仕様の明示                                            │
│  ─────────────────                                              │
│  「型定義を見れば仕様がわかる」                                  │
│  → Branded Types、Result型、Discriminated Union                 │
│                                                                 │
│  Level 3: 設計の表現  ★ 目指すレベル                            │
│  ─────────────────                                              │
│  「型で設計を強制し、違反をコンパイル時に検出」                  │
│  → 不変性、型の狭め、状態遷移の制約                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 型設計パターン

#### パターン1: Branded Types(IDの取り違え防止)

異なる種類のIDを同じ`string`型で扱うと、引数の順序ミスがコンパイル時に検出できない。

```typescript
// ❌ 危険:すべてstring型
function assignUser(orderId: string, userId: string) { }
assignUser(userId, orderId)  // 引数の順序ミス → 実行時まで気づかない

// ✅ 安全:専用の型(Branded Type)
type OrderId = string & { readonly __brand: 'OrderId' }
type UserId = string & { readonly __brand: 'UserId' }

function assignUser(orderId: OrderId, userId: UserId) { }
assignUser(userId, orderId)  // コンパイルエラー!
```

**ファクトリ関数の例:**

```typescript
function createUserId(id: string): UserId {
  // バリデーションロジックを追加可能
  if (!id.match(/^user_/)) {
    throw new Error('Invalid UserId format')
  }
  return id as UserId
}
```

#### パターン2: Discriminated Unions(状態の網羅的処理)

状態を持つデータは、Discriminated Unions(判別可能なUnion型)で表現する。
これにより、すべての状態を処理しないとコンパイルエラーになる。

```typescript
type AsyncState<T> = 
  | { status: 'idle' }
  | { status: 'loading' }
  | { status: 'success'; data: T }
  | { status: 'error'; error: Error }

function render(state: AsyncState<User>): string {
  switch (state.status) {
    case 'idle': return '待機中'
    case 'loading': return '読み込み中'
    case 'success': return state.data.name  // dataの型が確定
    case 'error': return state.error.message
    // すべてのケースを処理しないとコンパイルエラー
  }
}
```

**実践例:研修ステータスの型設計**

```typescript
// ❌ 危険:すべてのプロパティがoptional
type Training = {
  status: string
  startDate?: Date
  currentModule?: number
  completedAt?: Date
  score?: number
}
// → completedでないのにscoreにアクセスできてしまう

// ✅ 安全:状態ごとに利用可能なプロパティを型で制約
type Training = 
  | { status: 'draft' }
  | { status: 'scheduled'; startDate: Date }
  | { status: 'in_progress'; startDate: Date; currentModule: number }
  | { status: 'completed'; startDate: Date; completedAt: Date; score: number }

function getScore(training: Training): number | null {
  // training.score にアクセスするとエラー(全状態で存在しない)
  
  if (training.status === 'completed') {
    return training.score  // ✅ ここでのみscore利用可能
  }
  return null
}
```

**なぜこれが「設計を表現する」のか:**
- `draft`状態で`score`にアクセスしようとするとコンパイルエラー
- 状態遷移のルールが型で強制される
- Claudeが不正な状態アクセスを生成したら即座に検出

#### パターン3: Result型(例外を投げない)

例外(throw)は型システムで追跡できない。Result型を使うことで、エラーも戻り値として扱い、呼び出し側に処理を強制する。

```typescript
// Result型の定義
type Result<T, E> = 
  | { ok: true; value: T }
  | { ok: false; error: E }

// エラー型をDiscriminated Unionで定義
type LoginError = 
  | { type: 'InvalidCredentials' }
  | { type: 'AccountLocked'; unlocksAt: Date }
  | { type: 'NetworkError'; retryAfter: number }

// 戻り値でエラーを表現
async function login(
  email: string, 
  password: string
): Promise<Result<User, LoginError>> {
  // 成功時: { ok: true, value: user }
  // 失敗時: { ok: false, error: { type: 'InvalidCredentials' } }
}

// 呼び出し側は必ずエラーを処理する
const result = await login(email, password)
if (!result.ok) {
  switch (result.error.type) {
    case 'InvalidCredentials':
      return 'メールアドレスまたはパスワードが間違っています'
    case 'AccountLocked':
      return `アカウントがロックされています。${result.error.unlocksAt}以降に再試行してください`
    case 'NetworkError':
      return `ネットワークエラー。${result.error.retryAfter}秒後に再試行してください`
  }
}
// ここではresult.ok === trueが確定、result.valueにアクセス可能
```

### zodによるランタイム検証:Single Source of Truth

TypeScriptの型は**コンパイル時のみ**存在し、実行時には消える。
外部からの入力(API、ユーザー入力、ファイル読み込み)は、型で安全性を保証できない。

**zodの核心的価値:型とバリデーションの「単一真実源」**

```typescript
// ❌ 危険:型とバリデーションが分離(二重定義)
interface User {
  id: string
  email: string
  role: 'admin' | 'user'
}

function validateUser(data: unknown): boolean {
  // 型定義と別にバリデーションを書く → 乖離リスク
  return typeof data.id === 'string' && ...
}

// ✅ 安全:zodスキーマが唯一の定義源
import { z } from 'zod'

const UserSchema = z.object({
  id: z.string().uuid(),
  email: z.string().email(),
  role: z.enum(['admin', 'user']),
})

// 型を自動生成(スキーマと常に一致)
type User = z.infer<typeof UserSchema>

// バリデーションもスキーマから(二重定義なし)
const user = UserSchema.parse(data)
```

**なぜSingle Source of Truthが重要か:**

| 問題 | 分離されている場合 | zodで統一した場合 |
|------|-------------------|------------------|
| 型とバリデーションの乖離 | 起きる(手動で同期必要) | 起きない(自動生成) |
| 変更時の修正箇所 | 2箇所以上 | 1箇所のみ |
| Claudeへの指示 | 「型とバリデーション両方更新して」 | 「スキーマを更新して」 |

**zodの高度なパターン:**

```typescript
// 変換付きスキーマ(文字列→日付)
const EventSchema = z.object({
  name: z.string(),
  date: z.string().transform((s) => new Date(s)),
})

// 条件付きバリデーション
const PaymentSchema = z.discriminatedUnion('method', [
  z.object({ method: z.literal('card'), cardNumber: z.string() }),
  z.object({ method: z.literal('bank'), accountNumber: z.string() }),
])

// 部分的なスキーマの再利用
const BaseUserSchema = z.object({ name: z.string(), email: z.string().email() })
const AdminSchema = BaseUserSchema.extend({ permissions: z.array(z.string()) })
```

**CLAUDE.mdへの記載例:**

```markdown
## 型定義ルール
- 外部入力を受ける型は必ずzodスキーマで定義
- `type X = z.infer<typeof XSchema>` で型を生成
- interfaceの直接定義は内部型のみ許可
```

### Claudeへの型指示パターン

型を明示的に伝えることで、Claudeの出力品質が向上する。

```
「以下の型定義に厳密に従って実装してください。

【型定義】
type UserId = string & { readonly __brand: 'UserId' }
type TrainingId = string & { readonly __brand: 'TrainingId' }

type CreateUserInput = {
  readonly email: string
  readonly name: string
}

type CreateUserError = 
  | { type: 'EmailAlreadyExists' }
  | { type: 'ValidationError'; fields: string[] }

type Result<T, E> = 
  | { ok: true; value: T }
  | { ok: false; error: E }

type CreateUserResult = Result<User, CreateUserError>

【ルール】
- anyは絶対に使用禁止
- 戻り値型を必ず明示
- 例外をthrowせず、Result型で返す
- 引数にはreadonlyを付ける

実装後、typecheckを実行して結果を報告してください」
```

### tsconfig.json 推奨設定

厳格な型チェックを有効にすることで、Claudeが生成したコードの問題を早期に検出できる。

```json
{
  "compilerOptions": {
    // 必須:厳格モード
    "strict": true,
    
    // 配列アクセスの安全性(arr[0]がT | undefinedになる)
    "noUncheckedIndexedAccess": true,
    
    // optional propertyの厳格化
    "exactOptionalPropertyTypes": true,
    
    // 戻り値の網羅性チェック
    "noImplicitReturns": true,
    
    // switch文の網羅性チェック
    "noFallthroughCasesInSwitch": true
  }
}
```

### ESLint 型関連ルール

```json
{
  "rules": {
    "@typescript-eslint/no-explicit-any": "error",
    "@typescript-eslint/explicit-function-return-type": "error",
    "@typescript-eslint/no-unsafe-assignment": "error",
    "@typescript-eslint/no-unsafe-member-access": "error",
    "@typescript-eslint/no-unsafe-return": "error"
  }
}
```

### 型設計のチェックリスト

| 観点 | チェック項目 |
|------|-------------|
| ID型 | Branded Typeで取り違えを防止しているか |
| 状態 | Discriminated Unionで網羅的に処理しているか |
| エラー | Result型で戻り値として表現しているか |
| 外部入力 | zodでランタイム検証しているか |
| 設定 | tsconfig.jsonでstrict: trueか |

### 型設計パターン選択マトリクス

どの場面でどの型設計パターンを使うべきか、判断に迷うことがあります。
以下のマトリクスを使って、適切なパターンを選択してください。

#### パターン一覧と用途

| パターン | 主な用途 | 解決する問題 |
|---------|---------|-------------|
| **Branded Type** | ID、単位付き値 | 同じプリミティブ型の取り違え |
| **Discriminated Union** | 状態管理、イベント | 状態ごとの処理の網羅性 |
| **Result型** | エラーハンドリング | 例外の見落とし、エラー型の明示 |
| **zod** | 外部入力検証 | ランタイムでの型安全性 |
| **readonly** | 不変性保証 | 意図しない変更の防止 |
| **型ガード** | 型の狭め | unknown/any からの安全な変換 |

#### 選択マトリクス:「何を作るか」×「どのパターン」

```
┌─────────────────────────────────────────────────────────────────┐
│                型設計パターン選択マトリクス                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  作るもの              推奨パターン                              │
│  ─────────────         ──────────────                            │
│                                                                 │
│  エンティティのID      → Branded Type                           │
│  例: UserId, OrderId     取り違えをコンパイル時に検出            │
│                                                                 │
│  単位付きの値          → Branded Type                           │
│  例: Yen, Dollar, Kg     異なる単位の演算を防止                  │
│                                                                 │
│  画面/処理の状態       → Discriminated Union                    │
│  例: loading/success     switch文で網羅的に処理                  │
│                                                                 │
│  イベント/アクション   → Discriminated Union                    │
│  例: Created/Updated     イベントハンドラの型安全性              │
│                                                                 │
│  関数の戻り値(失敗可能)→ Result型                             │
│  例: 検索、保存           エラーを握りつぶさない                  │
│                                                                 │
│  API入力/レスポンス    → zod + z.infer                          │
│  例: リクエストボディ     ランタイム検証 + 型生成                 │
│                                                                 │
│  フォーム入力          → zod + z.infer                          │
│  例: ユーザー入力         バリデーションと型の一元管理            │
│                                                                 │
│  設定オブジェクト      → as const + readonly                    │
│  例: config, constants   不変性を保証                            │
│                                                                 │
│  外部ライブラリの型    → 型ガード + unknown                      │
│  例: JSON.parse結果      安全に型を狭める                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 判断フローチャート

```
┌─────────────────────────────────────────────────────────────────┐
│                型設計パターン 判断フロー                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Q1: 外部からの入力か?(API、フォーム、ファイル)               │
│      │                                                          │
│      ├─ Yes → zod を使う                                        │
│      │                                                          │
│      └─ No ↓                                                    │
│                                                                 │
│  Q2: 失敗する可能性があるか?(検索、保存、変換)                │
│      │                                                          │
│      ├─ Yes → Result型 を使う                                   │
│      │                                                          │
│      └─ No ↓                                                    │
│                                                                 │
│  Q3: 複数の状態を持つか?(loading/success/error など)          │
│      │                                                          │
│      ├─ Yes → Discriminated Union を使う                        │
│      │                                                          │
│      └─ No ↓                                                    │
│                                                                 │
│  Q4: 同じ型だが意味が違うか?(UserId vs OrderId)               │
│      │                                                          │
│      ├─ Yes → Branded Type を使う                               │
│      │                                                          │
│      └─ No → 通常の型定義でOK                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 組み合わせパターン

実際の開発では、複数のパターンを組み合わせて使います。

**例1: APIエンドポイント**

```typescript
// zod(入力検証)+ Result型(エラーハンドリング)+ Branded Type(ID)

const CreateUserInputSchema = z.object({
  email: z.string().email(),
  name: z.string().min(1),
})
type CreateUserInput = z.infer<typeof CreateUserInputSchema>

type CreateUserError = 
  | { type: 'EmailExists' }
  | { type: 'ValidationError'; message: string }

type UserId = string & { readonly __brand: 'UserId' }

async function createUser(
  input: CreateUserInput
): Promise<Result<UserId, CreateUserError>> {
  // 実装
}
```

**例2: 状態管理**

```typescript
// Discriminated Union(状態)+ Result型(データ取得)

type FetchState<T> = 
  | { status: 'idle' }
  | { status: 'loading' }
  | { status: 'success'; data: T }
  | { status: 'error'; error: Error }

// Result型でデータ取得
async function fetchUser(id: UserId): Promise<Result<User, FetchError>> {
  // 実装
}

// 状態更新
function updateState(result: Result<User, FetchError>): FetchState<User> {
  if (result.ok) {
    return { status: 'success', data: result.value }
  } else {
    return { status: 'error', error: result.error }
  }
}
```

#### よくある質問

| 質問 | 回答 |
|------|------|
| Branded Typeは全てのIDに必要? | 取り違えリスクがあるものに限定。1種類しかIDがない小規模プロジェクトでは不要 |
| zodは内部の型にも使う? | 外部入力のみ。内部の型定義はinterface/typeで十分 |
| Result型とtry-catchの使い分けは? | 予期されるエラー→Result型、予期しないエラー→try-catch |
| 全てのUnion型にdiscriminatorが必要? | 処理分岐が必要な場合のみ。単純な `string | null` には不要 |

> 💡 パターンの選択に迷ったら、まず「何を防ぎたいか」を明確にしてください。
> 防ぎたいバグがないなら、シンプルな型定義で十分です。

### 型の狭め(Narrowing):安全なアクセスの強制

Claudeが生成するコードで最も多いミスの一つが、**適切な型チェックの欠如**。
型の狭め(Narrowing)を理解し、CLAUDE.mdに明記することで防止できる。

```typescript
// ❌ 危険:nullチェックなし
function greet(user: User | null): string {
  return `Hello, ${user.name}`  // コンパイルエラー(strictモード)
}

// ✅ 安全:型の狭め
function greet(user: User | null): string {
  if (user === null) {
    return 'Hello, Guest'
  }
  return `Hello, ${user.name}`  // ここではUser型に狭まっている
}
```

**型の狭めパターン一覧:**

| パターン | 用途 | 例 |
|---------|------|-----|
| `=== null` / `!== null` | null/undefinedチェック | `if (x !== null)` |
| `typeof` | プリミティブ型の判別 | `if (typeof x === 'string')` |
| `in` 演算子 | プロパティの存在確認 | `if ('name' in x)` |
| `instanceof` | クラスインスタンスの判別 | `if (x instanceof Date)` |
| Discriminated Union | タグによる判別 | `if (x.status === 'success')` |
| `Array.isArray` | 配列判別 | `if (Array.isArray(x))` |
| カスタム型ガード | 複雑な判定ロジック | `if (isUser(x))` |

**カスタム型ガードの例:**

```typescript
// 型ガード関数(戻り値が `x is Type` 形式)
function isUser(x: unknown): x is User {
  return (
    typeof x === 'object' &&
    x !== null &&
    'id' in x &&
    'email' in x
  )
}

// 使用例
function processData(data: unknown): void {
  if (isUser(data)) {
    console.log(data.email)  // ✅ User型として扱える
  }
}
```

**CLAUDE.mdへの記載例:**

```markdown
## Type Safety Rules
- null/undefinedの可能性がある値は必ずチェックしてからアクセス
- 配列の要素アクセス後はundefinedチェック必須
- Discriminated Unionはswitch文で網羅的に処理
- unknownからの変換は型ガード関数を使用
```

### 不変性(Immutability):意図しない変更を防ぐ

Claudeが生成するコードで、オブジェクトや配列を意図せず変更してしまうバグが発生しやすい。
**readonlyを活用**することで、変更を型レベルで禁止できる。

```typescript
// ❌ 危険:ミュータブルな型
type User = {
  name: string
  roles: string[]
}

function addRole(user: User, role: string): void {
  user.roles.push(role)  // 元のオブジェクトを変更!
}

// ✅ 安全:イミュータブルな型
type User = {
  readonly name: string
  readonly roles: readonly string[]
}

function addRole(user: User, role: string): User {
  return {
    ...user,
    roles: [...user.roles, role]  // 新しいオブジェクトを返す
  }
}
```

**深い不変性(DeepReadonly):**

```typescript
// 再帰的にreadonlyを適用
type DeepReadonly<T> = {
  readonly [P in keyof T]: T[P] extends object
    — DeepReadonly<T[P]>
    : T[P]
}

type Config = DeepReadonly<{
  database: {
    host: string
    port: number
  }
}>

const config: Config = { database: { host: 'localhost', port: 5432 } }
config.database.port = 3000  // コンパイルエラー!
```

**Readonly ユーティリティ型:**

| 型 | 用途 |
|-----|------|
| `Readonly<T>` | 浅い不変性(1階層のみ) |
| `ReadonlyArray<T>` | 配列の不変性 |
| `as const` | リテラル型として推論(最も厳格) |
| `DeepReadonly<T>` | 深い不変性(自作またはライブラリ) |

### `as const`:リテラル型の威力

`as const`を使うと、値がリテラル型として推論され、より厳格な型チェックが可能になる。

```typescript
// ❌ 型が広がる
const config = {
  mode: 'production',
  features: ['auth', 'api']
}
// 型: { mode: string; features: string[] }

// ✅ 型が狭まる
const config = {
  mode: 'production',
  features: ['auth', 'api']
} as const
// 型: { readonly mode: 'production'; readonly features: readonly ['auth', 'api'] }
```

**`as const`の効果:**

| 観点 | `as const`なし | `as const`あり |
|------|---------------|----------------|
| 文字列 | `string` | リテラル型(`'production'`) |
| 配列 | `string[]` | タプル型(`readonly ['auth', 'api']`) |
| オブジェクト | 各プロパティが広い型 | 各プロパティがreadonly + リテラル |

**Claudeへの指示での活用:**

```
「設定オブジェクトは `as const` を付けて定義してください。
 これにより、誤った値の代入をコンパイル時に検出できます。」
```

### 型アサーション(as)の危険性

型アサーション(`as`)は型システムを「騙す」行為。
Claudeが `as any` や `as SomeType` を多用したコードを生成したら、設計に問題がある可能性が高い。

```typescript
// ❌ 危険:型アサーションで型チェックを回避
const user = data as User  // dataがUser型である保証がない

// ❌ 特に危険:any経由のアサーション
const user = data as any as User  // 何でも通ってしまう

// ✅ 安全:型ガードで検証してから使用
if (isUser(data)) {
  const user = data  // 型ガードを通過したので安全
}

// ✅ 安全:zodで検証
const user = UserSchema.parse(data)  // 実行時に検証
```

**型アサーションが許容されるケース:**

| ケース | 例 | 理由 |
|--------|-----|------|
| DOM操作 | `document.getElementById('x') as HTMLInputElement` | 開発者が要素の型を知っている |
| テストコード | `mockFunction as jest.Mock` | テスト用のモック |
| ファクトリ関数内 | `return id as UserId` | Branded Type作成時 |

**CLAUDE.mdへの記載:**

```markdown
## 禁止事項
- `as any` の使用は絶対禁止
- `as Type` は型ガードまたはzod検証の後のみ許可
- 型エラーを `as` で回避するのではなく、設計を見直す
```

### `noUncheckedIndexedAccess`:配列アクセスの安全性

この設定は**最も重要なtsconfig設定の一つ**だが、見落とされがち。

```typescript
const users = ['Alice', 'Bob']

// noUncheckedIndexedAccess: false(デフォルト)
const first = users[0]  // 型: string(危険!)
const tenth = users[10]  // 型: string(存在しないのに!)

// noUncheckedIndexedAccess: true
const first = users[0]  // 型: string | undefined
const tenth = users[10]  // 型: string | undefined

// 使用時にチェックが必須になる
if (first !== undefined) {
  console.log(first.toUpperCase())  // ✅ 安全
}
```

**なぜ重要か:**
- Claudeは配列の境界チェックを忘れやすい
- `users[0]`が常に存在する前提でコードを生成する
- この設定でコンパイル時に強制的にチェックさせる

**オブジェクトのインデックスアクセスにも適用:**

```typescript
const scores: Record<string, number> = { alice: 100 }

// noUncheckedIndexedAccess: true
const bobScore = scores['bob']  // 型: number | undefined
```

---

## 11. コロケーション(Colocation) [L1]

### 基本概念

```
Colocation = Co(一緒に)+ Location(配置)
原則: 関連するものは物理的に近くに配置する
```

### 従来型 vs コロケーション

```
┌─────────────────────────────────────────────────────────────────┐
│  ❌ 従来型(種類別配置)                                         │
│                                                                 │
│  src/                                                           │
│  ├── controllers/                                               │
│  │   └── trainingController.ts    ─┐                           │
│  ├── services/                      │                           │
│  │   └── trainingService.ts       ─┼─ 5つのディレクトリを       │
│  ├── repositories/                  │   行き来する必要がある     │
│  │   └── trainingRepository.ts    ─┤                           │
│  └── tests/                         │                           │
│      └── trainingService.test.ts  ─┘                           │
├─────────────────────────────────────────────────────────────────┤
│  ✅ コロケーション(機能別配置)                                 │
│                                                                 │
│  src/                                                           │
│  └── modules/                                                   │
│      └── training/                ─┐                           │
│          ├── training.controller.ts│                           │
│          ├── training.service.ts   ├─ 1つのディレクトリで完結   │
│          ├── training.repository.ts│                           │
│          ├── training.types.ts     │                           │
│          └── training.test.ts     ─┘                           │
└─────────────────────────────────────────────────────────────────┘
```

### コロケーションの適用レベル

| レベル | 内容 |
|--------|------|
| ディレクトリ構造 | 機能単位でファイルをグループ化 |
| テスト配置 | 実装ファイルの隣にテストを配置 |
| 型定義 | 使用箇所の近くに型を定義 |
| ドキュメント | 機能ディレクトリ内にREADME配置 |

### index.ts による公開API制御

```typescript
// src/modules/training/index.ts

// 公開するもの(他の機能から使える)
export { TrainingService } from './training.service'
export type { Training } from './training.types'

// 非公開(このモジュール内部のみ)
// - TrainingRepository
// - 内部のヘルパー関数
```

---

## 12. アーキテクチャとコロケーションの併用 [L2]

### 両者の関係

```
┌─────────────────────────────────────────────────────────────────┐
│                    アーキテクチャとコロケーション                 │
│                                                                 │
│  ┌─────────────────────┐      ┌─────────────────────┐          │
│  │   アーキテクチャ     │      │   コロケーション     │          │
│  │                     │      │                     │          │
│  │  「何を」「なぜ」    │      │  「どこに」配置     │          │
│  │   分けるか          │      │   するか            │          │
│  │                     │      │                     │          │
│  │  → 論理的な境界     │      │  → 物理的な構造     │          │
│  └──────────┬──────────┘      └──────────┬──────────┘          │
│             │                            │                      │
│             └────────────┬───────────────┘                      │
│                          ↓                                      │
│              ┌───────────────────────┐                          │
│              │       併用           │                          │
│              │                       │                          │
│              │ 論理的に正しく、      │                          │
│              │ 物理的にも扱いやすい  │                          │
│              └───────────────────────┘                          │
└─────────────────────────────────────────────────────────────────┘
```

### 統合設計例(Modular Monolith + Clean Architecture + コロケーション)

```
src/
└── modules/
    └── training/                    # モジュール境界(コロケーション)
        ├── README.md                # モジュール説明
        ├── domain/                  # Clean Architecture層
        │   ├── entities/
        │   │   ├── Training.ts
        │   │   └── Training.test.ts # テスト隣接(コロケーション)
        │   └── repositories/
        │       └── ITrainingRepository.ts
        ├── application/
        │   └── use-cases/
        │       ├── CreateTraining.ts
        │       └── CreateTraining.test.ts
        ├── infrastructure/
        │   └── repositories/
        │       └── TrainingRepository.ts
        ├── training.types.ts        # 型定義(コロケーション)
        └── index.ts                 # 公開API
```

### 併用時のルール

```
┌─────────────────────────────────────────────────────────────────┐
│                      併用時の3つのルール                         │
│                                                                 │
│  1. アーキテクチャの境界 > コロケーションの便利さ               │
│     └─ 便利だからと層の依存ルールを破らない                      │
│                                                                 │
│  2. コロケーションの単位 = アーキテクチャの単位                 │
│     └─ 一致させることで混乱を防ぐ                                │
│                                                                 │
│  3. 共有コード(shared/)は最小限に                             │
│     └─ 本当に複数モジュールで使うもののみ                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## Part 2 まとめ [L1]

```
┌─────────────────────────────────────────────────────────────────┐
│                  Part 2: 設計指針 まとめ                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐               │
│  │ドキュメント │ │モジュール   │ │アーキテクチャ│               │
│  │サイズ       │ │分割        │ │             │               │
│  ├─────────────┤ ├─────────────┤ ├─────────────┤               │
│  │ 1ファイル   │ │ 作業単位=  │ │ 明確な境界 │               │
│  │ 〜10,000字 │ │ ドキュメント│ │ 単方向依存 │               │
│  │ 会話合計    │ │ 単位       │ │ 小さな単位 │               │
│  │ 〜30,000字 │ │            │ │            │               │
│  └─────────────┘ └─────────────┘ └─────────────┘               │
│                                                                 │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐               │
│  │型設計指針   │ │コロケーション│ │コスト最適化│               │
│  ├─────────────┤ ├─────────────┤ ├─────────────┤               │
│  │ Branded Type│ │ 関連する    │ │ .claudeignore│              │
│  │ Result型   │ │ ものを近くに│ │ で除外     │               │
│  │ zod検証    │ │ 配置       │ │ 必要な情報 │               │
│  │ strict:true│ │            │ │ だけ読込   │               │
│  └─────────────┘ └─────────────┘ └─────────────┘               │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  核心メッセージ:                                                │
│  ━━━━━━━━━━━━━                                                  │
│  「ドキュメントは小さく、近くに」                                │
│  「型はClaudeへの仕様書 — 曖昧な言葉より正確に伝わる」           │
│  「論理的に正しく、物理的にも扱いやすく」                        │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  → Part 3(実践手法)では、日々の開発で使う具体的な              │
│    手法を解説します。                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

前のドキュメント: [Part 1: マインドセット](./01-mindset.md)
次のドキュメント: [Part 3: 実践手法](./03-practices.md)
